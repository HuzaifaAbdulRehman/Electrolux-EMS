# ===========================================
# Electrolux Energy Management System
# Environment Variables Configuration
# ===========================================

# -------------------------------------------
# Database Configuration (MySQL)
# -------------------------------------------
DB_HOST=localhost
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=electricity_ems

# Alternative: Use a single DATABASE_URL instead of discrete vars
# DATABASE_URL=mysql://USER:PASSWORD@localhost:3306/electricity_ems

# -------------------------------------------
# Application URLs
# -------------------------------------------
# Public API URL (used by frontend)
NEXT_PUBLIC_API_URL=http://localhost:3000

# NextAuth URL (must match your app's URL)
NEXTAUTH_URL=http://localhost:3000

# -------------------------------------------
# Authentication & Security
# -------------------------------------------
# NextAuth secret - generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here

# JWT secret for API authentication - generate with: openssl rand -hex 64
JWT_SECRET=your_jwt_secret_here

# -------------------------------------------
# Admin Registration
# -------------------------------------------
# Secret key required to register admin accounts
# Share only with authorized team members
ADMIN_SECRET_KEY=your_admin_secret_key_here

